doctype html
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario 
            h1= nombrePagina 
            p Agrega tu e-mail para restablecer la contraseña

            // Informamos los errores, si los hay
            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes) 
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error 

            form.caja-login(method="POST" action="/restablecer-password") 
                .campo 
                    label(for="email") Email: 
                    input(type="email" name="email" placeholder="Tu Email" value= email required)
                .campo.enviar 
                    input.boton(type="submit", value="Restablecer Password")
                .campo.acciones 
                    a(href="/iniciar-sesion") Volver a iniciar sesión 
