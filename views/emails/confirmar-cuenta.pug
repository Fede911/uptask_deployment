doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style 
            include ./email.css
    body
        h2 Confirmación de nueva cuenta en UpTask 
        p Ya casi estas registrado. Presiona el siguiente enlace.
        a(href=`${confirmarUrl}`) Restablecer Password 	   
        p Si no puedes acceder a este enlace, visita #{confirmarUrl}
